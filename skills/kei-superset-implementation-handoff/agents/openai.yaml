interface:
  display_name: "Kei Superset Implementation Handoff"
  short_description: "PromptとModelを確認後にSupersetへ委譲"
  default_prompt: "Use $kei-superset-implementation-handoff to preview the complete Agent Prompt, model, and reasoning, obtain my explicit approval, then launch it through Superset and verify real startup."
