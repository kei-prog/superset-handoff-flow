interface:
  display_name: "Kei Superset Implementation Handoff"
  short_description: "明確になった要件をSuperset実装agentへ渡す"
  default_prompt: "Use $kei-superset-implementation-handoff to validate the current task context with $kei-handoff, launch it through Superset, and verify real startup."
