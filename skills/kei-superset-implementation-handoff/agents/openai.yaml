interface:
  display_name: "Kei Superset Implementation Handoff"
  short_description: "実装指示とモデルを確認してSupersetへ安全に委譲"
  default_prompt: "$kei-superset-implementation-handoff を使い、子エージェントへ渡す実装指示全文、モデル、推論レベルを分かりやすい日本語で事前提示し、私の明示承認後にSupersetで起動して実行開始まで確認してください。"
